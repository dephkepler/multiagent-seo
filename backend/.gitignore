# secrets
internal/config/config.yaml
credentials.json
.env
.envrc.local

# MTProto session for the personal-account Telegram tools (cmd/tgsession) —
# this is full account access, more sensitive than the bot token in .env
tgsession.json
*.session

# local notes / workspaces
seo.txt
docs/kreya/

# go build output
contentflow
/server
/tgsession
*.exe
*.test
*.out
bin/
tmp/

# go tooling
coverage.out
coverage.html
*.prof
*.pprof

# macOS
.DS_Store

# IDE (if not already ignored globally)
.idea/
.vscode/

# claude code local state
.claude/
